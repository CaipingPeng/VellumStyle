- 新增大图自动压缩：超过微信 10 MiB 限制的 JPEG、PNG 图片会在保持原分辨率的前提下自动压缩，并尽量选择满足大小要求的最高画质，支持最大 50 MiB 的原图。
- 大幅提升大图压缩和上传速度，充分利用多核处理能力；正文图片、封面图、导入图片和素材库上传均使用统一的快速处理流程。
- 新增文章图片任务日志和正文占位状态，可查看读取、下载、压缩、上传、完成及失败进度；后台任务运行时仍可继续编辑文章和插入其他图片。
- 优化 Markdown 批量导入，文章创建后即可返回编辑，图片改为后台并发上传并逐张写回；同名文章不再覆盖原内容，导入失败时不会遗留半成品。
- 调整云同步策略，停止编辑约 3 分钟后自动同步，降低坚果云接口请求频率；支持通过 Ctrl+S（macOS 为 Cmd+S）立即保存并同步。
- 新增微信公众号永久图片素材库管理，可分页浏览、多选插入、批量上传、批量删除和手动刷新素材；删除前会明确提示草稿文章图片可能失效的风险。
- 修复图片选择窗口闪烁、重复窗口和大文件处理卡顿问题，并增强后台任务在文章切换、重命名、移动、删除及程序关闭时的可靠性。
- 新增 Linux x64 与 macOS Universal 自动构建，正式发布时可同时生成 Windows 安装包、Linux DEB/AppImage 和兼容 Intel/Apple Silicon 的 macOS DMG。
