- 新增命令面板，可通过快捷键搜索并执行视图切换、编辑、插入和应用操作。
- 新增可视化 Markdown 表格编辑器，支持快速选择行列、编辑单元格、调整对齐方式、增删行列和拖动列宽。
- 新增公式编辑器，提供 LaTeX 实时预览及按类别整理的常用公式与数学符号。
- 新增模板与常用片段库，可保存当前文章或自定义片段，并支持搜索、插入光标处和替换全文。
- 新增本地版本历史，支持查看逐行差异、恢复旧版本，并在文章或文件夹移动和重命名后保留历史。
- 重做文件树重命名交互：改名改为遮罩弹层输入，鼠标误点不再丢失已输入内容；只选中主文件名、文件夹也可双击改名、输入时即时校验非法字符/保留名/同级重名、失败保留输入框，`Esc` 真正取消而不再误提交，右键菜单新增「重命名」。
- 修复重命名后展开状态丢失、滚动位置跳动，以及嵌套文档改名无响应、改名输入框在改名成功后反复弹出的问题。
- 优化文档目录和大纲导航的宽度调整，拖拽条始终位于相邻卡片之间。
- 优化连续输入时的预览刷新，兼顾响应速度并避免预览长时间不更新。
- 修复命令面板在部分开发环境中打开后导致界面崩溃的问题。
