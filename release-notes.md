- 文件树重命名改为弹层输入：输入过程中鼠标点到别处不会再丢掉已输入的名字，只有按 Esc、点取消或明确放弃才会退出。
- 进入重命名时只选中主文件名，扩展名固定显示在输入框右侧，避免改错扩展名。
- 重命名输入时即时报错：非法字符、系统保留名、以点号或空格结尾、同级重名会在输入框下方直接说明，改到合法为止不会关闭输入框。
- 文件与文件夹现在都可以双击改名，右键菜单也新增了「重命名」，与 F2、悬停铅笔按钮共用同一套流程。
- 改名成功后树上的选中项、展开状态和滚动位置保持不变，文件夹改名不会再让里面的子项收起。
- 修复了文件夹内嵌套文档改名无反应，以及改名成功后输入框反复弹出的问题。
