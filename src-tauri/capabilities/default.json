{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限：主窗口可用核心 API",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
